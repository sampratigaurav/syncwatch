## 2024-06-14 - Accessible Icon Buttons
**Learning:** Icon-only buttons (like those in video players and chat interfaces) require both `aria-label` for screen readers and `title` for visual tooltips to ensure usability for all users.
**Action:** Always add descriptive `aria-label` and `title` attributes to buttons that only contain icons without visible text.
